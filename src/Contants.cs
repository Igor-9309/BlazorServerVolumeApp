namespace BlazorServerVolumeApp
{
    public static class Contants
    {
        public static class VolumeHubMethods
        {
            public const string ReLoadVolumeSync = nameof(ReLoadVolumeSync);

            public const string LoadVolumeSync = nameof(LoadVolumeSync);

            public const string SendVolumeSync = nameof(SendVolumeSync);

            public const string ReceiveVolumeSync = nameof(ReceiveVolumeSync);

            public const string FrontEndVolumeSync = nameof(FrontEndVolumeSync);
        }
    }
}
